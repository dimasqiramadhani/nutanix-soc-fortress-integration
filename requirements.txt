# Nutanix SOC Sandbox: Python Dependencies
#
# All scripts, namely generate_sample_logs.py, feed_logs.py,
# simulate_pipeline.py, and validate.py, use only the Python standard library
# version 3.8 or newer. There are no mandatory external dependencies.
#
# Optional, only for validating YAML during development:
#   pyyaml>=6.0
